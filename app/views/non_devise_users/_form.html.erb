<%= form_with(model: non_devise_user) do |form| %>
  <% if non_devise_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(non_devise_user.errors.count, "error") %> prohibited this non_devise_user from being saved:</h2>

      <ul>
        <% non_devise_user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :username %>
    <%= form.text_field :username %>
  </div>

  <div class="field">
    <%= form.label :password %>
    <%= form.text_field :password %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
