<%= form_with(model: user_award) do |form| %>
  <% if user_award.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_award.errors.count, "error") %> prohibited this user_award from being saved:</h2>

      <ul>
        <% user_award.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :year %>
    <%= form.number_field :year %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
